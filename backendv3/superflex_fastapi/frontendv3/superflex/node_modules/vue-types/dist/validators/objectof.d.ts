import { Prop, VueProp, InferType } from 'vue-types/dist/types';
export default function objectOf<T extends VueProp<any> | Prop<any>>(type: T): import("vue-types/dist/types").VueTypeDef<{
    [key: string]: InferType<T>;
}>;
